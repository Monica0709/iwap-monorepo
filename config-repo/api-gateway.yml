server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: test_route
          uri: http://httpbin.org
          predicates:
            - Path=/get

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
